# G07 staging — prove reali parziali

Data: 2026-09-07. Sito: `https://staging.yogabuenaonda.it` (Hostinger).
Autorizzazione utente: modifiche temporanee shared e ripristino sullo staging.
Baseline repository: main `6762d7e9d1c4a1be8bce059b696a89b24758c054`.

## Metodo

Backup completi dei dati Elementor conservati privatamente prima delle modifiche, esclusi dal repository. Salvataggi e rollback via `POST /wpvibe/v1/elementor/save-template`, preservando ID, tipo, stato publish e condizioni. Nessuna scrittura diretta su `_elementor_data`, nessuna modifica al guard SeoGrow `sharedWriteAllowed=false`.

Confronto dei dati letti dopo il salvataggio e dopo il ripristino, normalizzando solo l'ordine delle chiavi JSON. Le letture frontend provengono dal browser della sessione; non sono prove eseguite dal runtime SeoGrow.

| Caso | Prova eseguita | Esito |
| --- | --- | --- |
| A Header 185 | Testo `QA G07` nel bottone esistente; visibile in homepage e post 7197; rollback dati esatto e marker assente su entrambi | PASS nel perimetro provato |
| B Footer 327 | Testo `QA G07 FOOTER`; visibile su entrambe le pagine; rollback dati esatto e marker assente | PASS nel perimetro provato |
| C Single 598 | Condizione include/singular/post letta; nei post 7197 e 7170 il DOM mostra wp-post, senza documento Single 598 | Non eseguito save; serve un contenuto su cui il template sia realmente applicato |
| D Archive 584 | Classe innocua qa-g07-archive su struttura esistente; presente nella categoria 38 e assente in homepage; rollback esatto e marker assente | PASS nel perimetro provato |
| E Shared/global | Propagazione header/footer verificata su due pagine | PARTIAL: non tutte le referenze/global widget |
| F Custom CSS | Custom property --qa-g07 persistita nel widget HTML 8a7f295, valore computed vuoto; seconda prova outline-offset 0.731px restituisce 0px anche dopo object cache flush | NON SUPERATO; causa tra generazione/serving/cache non isolata, nessuna attribuzione a SeoGrow |
| G HTML/script | Span hidden con data-qa-g07 nel widget HTML esistente, nessun nuovo script; persistito e presente nel DOM delle due pagine; assente dopo rollback | PASS solo marker HTML non eseguibile; capability/script non certificati |
| H CPT | Elenco REST tipi letto; nessun CPT di contenuto rappresentativo selezionato | Non eseguito |
| I Cache/asset | HTML fresco dopo save e rollback; pipeline ufficiale senza warnings; CSS non applicato | PARTIAL |
| J Visual | Baseline desktop osservata, verifiche DOM multi-pagina | PARTIAL: manca matrice visuale completa before/after/rollback tablet/mobile |

## Scope e ripristino

- Header/footer: condizioni `include/general` conservate dalle risposte save/rollback, warnings vuoti.
- Archive: `include/archive` conservato, warnings vuoti.
- Homepage `/`, articolo `/yoga-online-o-presenza-cinisello-balsamo/`, archivio `/category/yoga-cinisello-balsamo/`.
- Template Single 598 e section 2789 letti senza modifiche.
- Nessun contenuto live modificato, nessun utente creato, nessun form o link di contatto attivato.
- Staging inizialmente `blog_public=1`: impostato a `0`, robots `nofollow, noindex` verificato. Questa impostazione protettiva resta applicata; non fa parte dei marcatori ripristinati.

## Limiti

G07 resta **PARTIAL**, nonostante i singoli casi superati. Queste prove certificano la pipeline WPVibe/Elementor sul clone nei casi descritti, non abilitano né certificano write shared attraverso SeoGrow. L'interfaccia browser disponibile non espone un controllo documentato delle dimensioni viewport: nessuna simulazione tablet/mobile dichiarata. Restano da isolare il problema CSS e i fixture/scope mancanti prima della chiusura.

## Ripresa dopo autorizzazione esplicita G07

L’utente ha confermato esplicitamente «Autorizzo Go7» dopo il rifiuto automatico di un ulteriore test. Il test è stato quindi rieseguito sul solo staging.

- Header 185, widget nav-menu 585f51bc: aggiunta temporanea `outline-offset: 0.731px` al custom CSS esistente. Valore computed `1px` nell’editor e sul frontend autenticato; dopo rollback `0px`. Dati originali verificati esatti e condizioni conservate. Il CSS in questa sessione è caricato con query di versione.
- Single 598: salvata classe temporanea `qa-g07-single`, dati persistiti conformi; post 7197 continua a mostrare documento `wp-post` senza Single 598 e senza marker. Rollback esatto verificato. Il salvataggio non dimostra applicazione delle condizioni: caso C non superato.
- HTML anonimo: asset aggregato LiteSpeed diverso dal CSS versionato autenticato. `cache purge` ha confermato pulizia LiteSpeed, Elementor CSS e object cache sul clone. L’asset aggregato generato dopo il purge ha restituito inizialmente CSS (lettura troncata), poi alla richiesta completa una pagina «This Page Does Not Exist». Nessun PASS per CSS/cache anonima, né attribuzione definitiva a LiteSpeed, hosting o browser. Ripristino header e nuovo purge completati.
- Accesso sicuro all’editor Elementor riuscito. Le anteprime responsive interne sono disponibili; letture post-rollback: larghezza/scrollWidth 753/753 in tablet e 345/345 in mobile. Osservata anteprima mobile con hamburger. Non è la matrice completa before/after/rollback, né una verifica di tutte le pagine.
- Verifica finale: header 185 e Single 598 identici ai backup; frontend senza marker, outline-offset 0px, robots nofollow/noindex. Nessuna modifica al live.

**Stato G07: PARTIAL.** Il CSS autenticato è ora verificato; rimangono cache/asset anonimi, applicazione Single, copertura shared/global e CPT rappresentativo, matrice visuale completa. Riprendere dalla diagnosi di questi casi senza allargare le condizioni del template sul cliente o disabilitare cache/guardie per ottenere un PASS artificiale.
