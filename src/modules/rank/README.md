# Rank & Growth module

This directory owns Rank & Growth domain behavior as it is extracted from the legacy shell.

Current ownership includes opportunity-task creation and reuse logic. Legacy top-level entry points may remain as compatibility shims during migration, but new production consumers should depend on `modules/rank/index.js`.
