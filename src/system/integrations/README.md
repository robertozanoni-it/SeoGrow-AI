# System integrations

This directory owns integration-level state and normalization behavior used by the SeoGrow Suite system layer.

Google property/status normalization is owned here. Legacy top-level entry points remain temporary compatibility shims; new consumers should use `system/index.js`.
